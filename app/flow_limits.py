"""Deployment defaults shared by worker scheduling and the stdlib installer."""

MAX_SLOTS = 32
DEFAULT_TOTAL_CAPACITY = 12
DEFAULT_HEADLESS_CAPACITY = 12
DEFAULT_HEADED_CAPACITY = 1
DEFAULT_PORTAL_CAPACITY = 4
